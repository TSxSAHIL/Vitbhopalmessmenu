import 'package:flutter/material.dart';

class NightCanteen extends StatelessWidget {
  const NightCanteen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}